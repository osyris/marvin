# Исследование: Создание премиального лендинга B2B SaaS через Claude Code (Январь-Февраль 2026)

> Дата исследования: 11 февраля 2026
> Продукт: B2B SaaS — голосовые AI-ассистенты

---

## 1. Текущий стек для премиальных лендингов (2026)

### Фреймворки

| Фреймворк | Лучше для | Особенности 2026 |
|-----------|-----------|------------------|
| **Next.js 15+** | Интерактивные SaaS-сайты, full-stack | App Router, Server Components, Turbopack, ISR. Экосистема shadcn/ui. **Рекомендация #1 для B2B SaaS** |
| **Astro 4+** | Контентные/маркетинговые сайты | Islands architecture, 0 JS по умолчанию, быстрейший performance. Отлично для лендингов с минимальной интерактивностью |
| **Nuxt 4** | Vue-экосистема | Hybrid rendering, хорош, но экосистема компонентов меньше чем у React |
| **SvelteKit** | Максимальный DX и performance | Компактнее всех, но меньше готовых UI-библиотек |

**Вердикт:** Для B2B SaaS с анимациями и интерактивностью → **Next.js**. Экосистема React даёт доступ к Framer Motion, Aceternity UI, Magic UI, shadcn/ui — всему, что нужно для премиального сайта. Astro — отличная альтернатива если нужен максимальный performance и меньше интерактивности.

### shadcn/ui

- **Не библиотека, а набор copy-paste компонентов** — полный контроль над кодом
- Построен на Radix UI (доступность) + Tailwind CSS
- 50+ компонентов: Dialog, Sheet, Command, Tabs, Accordion, и т.д.
- **shadcn/ui Blocks** — готовые секции (dashboard, auth, sidebar)
- CLI: `npx shadcn@latest add button` — мгновенная установка
- Идеально интегрируется с Next.js, полностью кастомизируемый
- **Сам по себе НЕ для лендингов** — но в комбинации с Aceternity/Magic UI даёт всё

### Другие UI-библиотеки на пике

- **Radix UI** — headless примитивы, база shadcn/ui. Accessibility из коробки
- **Ark UI** — от создателей Chakra UI, headless, мультифреймворковый (React, Vue, Solid)
- **Park UI** — стилизованные компоненты поверх Ark UI (аналог shadcn для Ark)
- **DaisyUI** — Tailwind-based, простой, хорош для быстрого старта
- **UIverse** — community-driven коллекция UI-элементов (Pinterest для компонентов)
- **Float UI** — готовые секции для SaaS (hero, pricing, features)

### Tailwind CSS v4

Релиз: **22 января 2025**. Ключевые изменения:

- **Oxide Engine** — движок на Rust, **до 100x быстрее билды**
- **CSS-first конфигурация** — `@theme` вместо `tailwind.config.js`, настройка прямо в CSS
- **Нативные CSS layers** — `@layer theme, base, components, utilities`
- **Контейнерные запросы** — `@container` из коробки
- **Широкие цветовые гаммы** — P3 цвета, oklch()
- **Новые утилиты**: `text-wrap: balance`, `@starting-style`, улучшенные градиенты
- **Автоматический content detection** — не нужно указывать paths в конфиге
- **Zero-config** подход — просто `@import "tailwindcss"` в CSS

---

## 2. Анимации и Motion

### Framer Motion → Motion (for React)

- Переименован в **Motion** (motion.dev), остаётся #1 для React-анимаций
- **Декларативный API**: `<motion.div animate={{opacity: 1}} />`
- Layout animations, AnimatePresence, gesture support (drag, hover, tap)
- **Scroll-triggered анимации**: `useScroll`, `useInView`, `whileInView`
- Отлично работает с Next.js App Router и Server Components
- **Рекомендация: основная библиотека для анимаций**

### GSAP (GreenSock)

- **Всё ещё актуален и мощен** — особенно для сложных timeline-анимаций
- ScrollTrigger — мощнейший плагин для scroll-анимаций
- **Плюсы**: максимальный контроль, complex timelines, framework-agnostic
- **Минусы**: императивный стиль, сложнее интеграция с React (useRef, useEffect, cleanup)
- **Когда использовать**: сложные последовательности, parallax, pinning секций
- Лицензия: бесплатен для большинства случаев, но premium плагины платные

### Lottie

- Идеален для **сложных иконных анимаций**, загрузчиков, микро-анимаций
- Экспорт из After Effects → JSON → рендер на вебе
- **lottie-react** / **@lottiefiles/dotlottie-react** — React-компоненты
- LottieFiles.com — библиотека готовых анимаций
- Отлично для: animated icons, loading states, success/error animations

### CSS-анимации (без библиотек)

В 2026 CSS стал невероятно мощным:

- **@keyframes** + **animation** — базовые анимации
- **Scroll-Driven Animations** (`animation-timeline: scroll()`) — **нативный CSS!** Поддержка Chrome, Edge, Safari 18+
- **View Transitions API** — нативные page transitions без JS. Поддержка Chrome, Safari (с октября 2025). Замена JS-библиотек для page transitions
- **@starting-style** — анимация появления элементов
- **CSS `transition-behavior: allow-discrete`** — анимация display:none↔block
- **Tailwind CSS Motion** (`tailwindcss-motion`) — утилитарные классы для анимаций

### Библиотеки для scroll-triggered анимаций

| Библиотека | Описание |
|-----------|----------|
| **Motion (Framer Motion)** `whileInView` | Простейший способ, декларативный |
| **GSAP ScrollTrigger** | Самый мощный, pinning, parallax, complex timelines |
| **CSS Scroll-Driven Animations** | Нативный CSS, zero JS, но ограниченный контроль |
| **AOS (Animate On Scroll)** | Простой, data-атрибуты, но менее гибкий |
| **Lenis** | Smooth scrolling библиотека, часто используется вместе с GSAP |

### Animated backgrounds, gradients, particles

| Эффект | Библиотека / Подход |
|--------|---------------------|
| **Particle effects** | `tsparticles` (наследник particles.js), `react-particles` |
| **Mesh gradients** | CSS `conic-gradient` + `filter: blur()`, или Canvas-based решения |
| **Animated gradients** | CSS `@keyframes` + `background-position`, Aceternity UI Spotlight |
| **Aurora/Glow effects** | Aceternity UI Aurora Background, CSS radial-gradient + animation |
| **3D effects** | Three.js + React Three Fiber (для hero-секций), `@react-three/drei` |
| **Noise/Grain** | CSS `filter: url(#noise)` SVG filter, или Canvas |
| **Dot patterns** | CSS `radial-gradient` pattern |
| **Grid backgrounds** | CSS `linear-gradient` patterns (Aceternity UI Grid Background) |

---

## 3. Визуальный дизайн

### Паттерны лендингов B2B SaaS 2026

На основе анализа SaaSFrame и лучших примеров:

1. **Story-Driven Hero Sections** — не просто заголовок, а нарративная подача. Показываем проблему → решение в первом экране
2. **Персонализированные CTA** — динамические, benefit-driven ("Попробуй для своей команды"), не generic "Sign up"
3. **Микроанимации с целью** — каждая анимация демонстрирует функциональность, не декорация
4. **Immersive Product Previews** — интерактивные демо прямо на странице, видео-демо в hero
5. **Social Proof секции** — логотипы клиентов, цитаты, числа. Прямо рядом с CTA
6. **Bento Grid Features** — модульная сетка для показа возможностей
7. **Dark mode по умолчанию** — для dev-tools и AI-продуктов тёмная тема = премиальность
8. **Product-led storytelling** — показывать интерфейс продукта, а не stock-фото
9. **Speed & Performance** — быстрая загрузка = доверие
10. **AI-персонализация** — адаптивный контент под сегмент посетителя

### Дизайн-тренды 2026

| Тренд | Статус | Как использовать |
|-------|--------|------------------|
| **Bento Grids** | 🔥 Пик популярности | Feature секции, comparison, capabilities. CSS Grid с разными размерами ячеек |
| **Glassmorphism** | ✅ Эволюционировал | "Frosted glass" + динамический blur при скролле. Subtle, не overused |
| **Mesh Gradients** | ✅ Актуально | Background hero-секций, accent элементы. Animated для премиальности |
| **Aurora/Glow** | 🔥 Тренд | Glow effects вокруг карточек, CTA-кнопок, header |
| **Grain/Noise текстуры** | ✅ Актуально | Subtle texture поверх градиентов, добавляет тактильность |
| **Organic shapes** | ✅ Актуально | Blob-формы, wavy dividers между секциями |
| **3D elements** | ⚠️ Осторожно | Только точечно (hero, product preview), не перегружать |
| **Brutalism** | ❌ Не для B2B SaaS | Слишком рискованно для корпоративной аудитории |

### Типографика

**Топ шрифты для SaaS лендингов 2026:**

- **Inter** — универсальный sans-serif, отличная читаемость. Используют: Linear, Vercel
- **Geist** — от Vercel, modern geometric sans. Next.js по умолчанию
- **Cal Sans** — display шрифт от Cal.com, для заголовков
- **Satoshi** — modern geometric, популярен в SaaS
- **Plus Jakarta Sans** — warm geometric, хорош для B2B
- **General Sans** — clean, professional
- **Instrument Sans/Serif** — Google Font, versatile

**Подходы:**
- Комбинация: display font (заголовки) + body font (текст)
- Размеры героя: 48-72px (desktop), 32-48px (mobile)
- Line-height: 1.1-1.2 для заголовков, 1.5-1.7 для текста
- `font-feature-settings: "ss01"` — стилистические альтернативы для уникальности

### Цветовые схемы

**Для AI/Voice SaaS продукта:**

- **Dark mode** как основной — ассоциация с tech, AI, premium
- Основной фон: `#0A0A0A` или `#09090B` (не чисто чёрный!)
- Акцентные цвета: фиолетовый (#7C3AED), синий (#3B82F6), или градиент purple→blue
- Glow effects: акцентный цвет с opacity 20-30%
- Текст: белый (#FAFAFA) для заголовков, серый (#A1A1AA) для body
- Границы карточек: `border-white/10` (subtle, transparent)
- **Градиенты**: `from-purple-500 via-blue-500 to-cyan-500` — AI-ассоциация

**Light mode (вторичный):**
- Фон: `#FAFAFA`, карточки: `#FFFFFF`
- Акцент: тот же, но насыщеннее
- Borders: `#E5E7EB`

### Где брать вдохновение

| Ресурс | Для чего |
|--------|----------|
| **SaaSFrame.io** | Коллекция SaaS лендингов, отсортированная по секциям |
| **Awwwards.com** | Лучшие сайты мира, эталон качества |
| **Land-book.com** | Коллекция лендингов |
| **Dribbble.com** | UI/UX дизайн-концепты |
| **Mobbin.com** | Скриншоты UI паттернов |
| **Godly.website** | Подборка божественных сайтов |
| **Dark.design** | Dark-mode сайты |
| **Minimal Gallery** | Минималистичные сайты |
| **Refero.design** | Реальные продуктовые интерфейсы |

---

## 4. Готовые шаблоны и библиотеки компонентов

### Aceternity UI

- **70+ анимированных компонентов** на React + Next.js + Tailwind + Framer Motion
- Copy-paste или установка через shadcn CLI
- **Ключевые компоненты для лендингов:**
  - Hero sections с animated backgrounds
  - Spotlight effect, Aurora Background, Grid Background
  - Animated cards, Bento Grid, 3D Card Effect
  - Text Generate Effect, Typewriter Effect
  - Floating Navbar, Animated Testimonials
  - Lamp Effect, Moving Border, Background Beams
  - Tracing Beam (scroll progress)
- **Бесплатная версия** — базовые компоненты. **Pro** — расширенные
- **Идеален для "вау-эффекта"** на лендингах
- Сайт: ui.aceternity.com

### Magic UI

- **50+ animated components** в стиле shadcn/ui
- Copy-paste философия, полная кастомизация
- **Ключевые компоненты:**
  - Animated Beam (connections visualization)
  - Globe (3D глобус)
  - Marquee (бегущая строка логотипов)
  - Number Ticker (animated counters)
  - Shimmer Button, Shine Border
  - Particles, Dot Pattern
  - Animated List, Dock (macOS-style)
  - Border Beam, Meteors
- Сайт: magicui.design
- **Бесплатный**, open-source

### Другие библиотеки

| Библиотека | Описание |
|-----------|----------|
| **Luxe Components** | Premium animated components, shadcn-compatible |
| **Cult UI** | Animated copy-paste components |
| **Animata** | Hand-crafted animated components |
| **Ibelick** | Animated backgrounds и effects |
| **Float UI** | Готовые секции для SaaS (hero, pricing, features) |
| **Tailwind UI** (платный) | Официальные premium компоненты от Tailwind Labs |
| **Shadcn Blocks** | Готовые секции (sidebar, auth, dashboard) |
| **Origin UI** | Beautiful UI components |

### Готовые landing page шаблоны на shadcn

- **shadcn.io** — каталог шаблонов: Luro AI (SaaS landing), и другие
- **Taxonomy** (от shadcn) — Next.js app template
- **SaaS starter kits**: next-saas-stripe-starter, и подобные
- Комбинация: shadcn/ui (базовые) + Aceternity (анимации) + Magic UI (эффекты) = **полный набор**

---

## 5. Pipeline: от брифа до готового сайта через Claude Code

### Структура брифа для Claude Code

```markdown
# Бриф: Лендинг [Название продукта]

## Продукт
- Что это: [описание]
- Для кого (ICP): [целевая аудитория]
- Главная ценность: [value proposition]
- Ключевые features: [3-5 features]
- Конкуренты: [ссылки]

## Дизайн
- Стиль: premium, dark mode, AI-focused
- Вдохновение: [ссылки на 3-5 сайтов]
- Цветовая палитра: [цвета]
- Шрифты: [основной + display]
- Настроение: futuristic, trustworthy, innovative

## Структура страницы
1. Hero: [описание]
2. Social Proof: [логотипы, числа]
3. Features: [bento grid]
4. How it works: [шаги]
5. Demo/Preview: [интерактивное демо]
6. Testimonials: [отзывы]
7. Pricing: [тарифы]
8. CTA: [финальный призыв]
9. Footer

## Технические требования
- Стек: Next.js 15, Tailwind v4, shadcn/ui, Aceternity UI, Motion
- Mobile-first responsive
- Performance: 90+ Lighthouse
- SEO: мета-теги, OG-images, structured data
```

### Оптимальный порядок действий

#### Этап 1: Планирование (Claude Code)
```
Промпт: "Создай структуру лендинга для B2B SaaS продукта — голосовые AI-ассистенты. 
Определи: sitemap, wireframe каждой секции (текстовое описание), копирайтинг для каждого блока."
```
- Claude Code генерирует текстовый wireframe + copy
- Итерируем до идеала

#### Этап 2: Инициализация проекта
```bash
npx create-next-app@latest my-landing --typescript --tailwind --eslint --app --src-dir
cd my-landing
npx shadcn@latest init
```
Промпт Claude Code:
```
"Настрой проект: установи зависимости (framer-motion, @next/font), 
настрой Tailwind v4 конфиг с цветовой палитрой [цвета], 
добавь шрифты Inter + Cal Sans, настрой dark mode."
```

#### Этап 3: Дизайн-система
```
"Создай дизайн-систему:
- globals.css с CSS variables (цвета, spacing, typography)
- Компоненты: Button (primary, secondary, ghost), Card, Badge, Container
- Утилитарные классы для градиентов, glow-эффектов
- Responsive breakpoints"
```

#### Этап 4: Поэтапная сборка секций
**Каждую секцию — отдельным промптом!** Не просить весь сайт сразу.

```
"Создай Hero секцию: 
- Анимированный заголовок (text-generate effect из Aceternity UI)
- Подзаголовок с fade-in
- Два CTA-кнопки: primary (gradient) и secondary (outline)
- Фоновый эффект: grid pattern + spotlight glow
- Animated badge сверху: 'Новинка: GPT-4o интеграция'"
```

Порядок секций:
1. **Navbar** (floating, blur backdrop)
2. **Hero** (animated headline, CTA, background effects)
3. **Logos/Social Proof** (marquee с логотипами клиентов)
4. **Features** (bento grid с анимированными карточками)
5. **How It Works** (steps с animated connectors)
6. **Interactive Demo** (product preview / audio player)
7. **Testimonials** (animated carousel)
8. **Pricing** (3 columns, popular highlighted)
9. **FAQ** (accordion)
10. **Final CTA** (strong call to action + background gradient)
11. **Footer**

#### Этап 5: Анимации и polish
```
"Добавь анимации:
- Все секции: fade-in при скролле (whileInView)
- Hero: staggered text animation
- Feature cards: hover scale + glow border
- Numbers: counting animation (число от 0 до N)
- Smooth scroll (Lenis)
- Page transition effects"
```

#### Этап 6: Оптимизация
```
"Оптимизируй:
- Все изображения: next/image, WebP, lazy loading
- Fonts: next/font/google, font-display: swap
- Bundle: dynamic imports для тяжёлых компонентов (particles, 3D)
- Metadata: generateMetadata(), OG images, JSON-LD
- Mobile: проверь все breakpoints, touch targets 44px+
- Accessibility: aria-labels, keyboard navigation, skip-links
- Performance: aim for 90+ Lighthouse"
```

### .claude/ конфигурация

Создайте `CLAUDE.md` в корне проекта:

```markdown
# CLAUDE.md

## Проект
Премиальный лендинг для B2B SaaS (голосовые AI-ассистенты).

## Стек
- Next.js 15 (App Router)
- TypeScript (strict)
- Tailwind CSS v4
- shadcn/ui (для базовых компонентов)
- Framer Motion / Motion (для анимаций)
- Aceternity UI (для animated landing components)
- Magic UI (для дополнительных эффектов)

## Стиль кода
- Функциональные компоненты
- "use client" только где реально нужно
- Server Components по умолчанию
- Именование: PascalCase для компонентов, camelCase для функций
- Файловая структура: src/components/sections/, src/components/ui/, src/lib/

## Дизайн-правила
- Dark mode первичный
- Цвета: см. globals.css CSS variables
- Spacing: 8px система (Tailwind default)
- Анимации: subtle, purposeful, 200-500ms duration
- Не более 3 анимаций одновременно на экране
- Mobile-first подход

## Не делать
- Не использовать "use client" без необходимости
- Не загружать тяжёлые библиотеки в initial bundle
- Не использовать inline styles
- Не хардкодить текст (вынести в constants)
```

### MCP-серверы для дизайна

| MCP Server | Назначение |
|-----------|-----------|
| **Figma MCP Server** (официальный) | Передаёт дизайн-контекст из Figma → Claude Code. Frames, tokens, layout constraints |
| **Framelink MCP** | Альтернативный Figma MCP, читает данные через Figma API |
| **Browser MCP** | Просмотр результата, скриншоты для итерации |
| **21st.dev Magic MCP** | Генерация UI-компонентов в стиле shadcn |

**Рекомендуемый workflow с Figma MCP:**
1. Создать макет в Figma (или попросить дизайнера)
2. Подключить Figma MCP к Claude Code
3. Claude Code читает дизайн-токены, структуру, размеры
4. Генерирует код, точно соответствующий макету

---

## 6. AI-инструменты для дизайна (2026)

### v0.dev (Vercel)

- **Лучший AI-генератор UI компонентов**
- Генерирует React + Tailwind + shadcn/ui код из промпта
- Превратился в **v0.app** — полноценная платформа для создания и деплоя сайтов
- **Сила**: красивый, чистый UI-код. Отлично для отдельных секций
- **Слабость**: нет backend, привязка к Vercel
- **Использование в pipeline**: сгенерировать секцию в v0 → скопировать в проект → доработать в Claude Code

### bolt.new (StackBlitz)

- Полный full-stack dev в браузере из промпта
- Мгновенный preview, one-click deploy
- Хорош для **быстрого прототипирования**
- Менее качественный код чем v0 для UI

### Lovable.dev

- Быстрейший full-stack MVP builder
- Генерирует frontend + backend + DB из промпта
- Лучше для приложений, чем для лендингов
- Supabase интеграция из коробки

### Claude Artifacts

- Можно прототипировать React-компоненты прямо в чате
- **Ограничение**: нет доступа к npm-пакетам (Tailwind через CDN, нет Framer Motion)
- **Использование**: быстрые визуальные эксперименты, layout-проверки
- Потом перенос в Claude Code для production-кода

### Figma AI + MCP

- **Figma MCP Server** — мост между дизайном и кодом
- **Code Connect** — связывает Figma-компоненты с React-компонентами в коде
- **Dev Mode** — Figma показывает CSS/design tokens для разработчиков
- **AI-powered features** в Figma: auto-layout suggestions, generate variants

### Screenshot-to-Code

- **screenshot-to-code** (open source) — скриншот → HTML/React код
- **v0.dev** — можно загрузить скриншот как референс
- Claude Code с browser MCP — может "увидеть" скриншот и воспроизвести

---

## 7. Эталонные B2B SaaS лендинги

### Linear.app
- **Что делает крутым**: Абсолютный минимализм + качественные анимации. Dark mode. Плавные transitions. Product screenshots как главный visual. Типографика Inter. Каждая анимация — с целью (демонстрирует speed продукта)
- **Паттерны**: Hero с продуктовым скриншотом, Changelog как маркетинг, Features через визуальные демо

### Vercel.com
- **Что делает крутым**: Dark theme, gradient accents, particle/grid backgrounds. "DX-first" messaging. Interactive code examples. Бесконечно гладкие анимации. Шрифт Geist
- **Паттерны**: Hero с animated gradient, Framework logos marquee, Performance metrics section, Deploy demo

### Resend.com
- **Что делает крутым**: Чистейший дизайн, код как визуальный элемент, минимальные но точные анимации. Показывают API прямо на лендинге
- **Паттерны**: Code snippet в hero, Dark mode, Minimal layout, Developer-focused copy

### Clerk.com
- **Что делает крутым**: Интерактивный UI в hero (показывает auth flow), Bento grid features, Clean dark/light toggle
- **Паттерны**: Product demo in hero, Component showcase, Integration logos

### Другие эталоны

| Сайт | Что взять |
|------|-----------|
| **Stripe.com** | Анимированные gradient meshes, интерактивные demos, безупречная типографика |
| **Raycast.com** | Command palette как hero, keyboard-first UX, dark theme |
| **Supabase.com** | Developer-focused, code examples, dark with green accents |
| **Planetscale.com** | Grid backgrounds, animated borders, tech aesthetic |
| **Lemon Squeezy** | Playful но professional, яркие градиенты, fun animations |
| **Cal.com** | Open-source гордость, clean layout, Cal Sans font |

### Общие паттерны лучших сайтов

1. **Dark mode** как основной (90% dev-tools/AI)
2. **Минимализм** — меньше элементов, но каждый качественный
3. **Product first** — показывают продукт, не stock-фото
4. **Subtle animations** — ничего не прыгает в лицо, всё плавно
5. **Strong typography** — большие заголовки, чёткая иерархия
6. **Social proof** — логотипы рядом с hero, не спрятаны внизу
7. **Performance** — сайты летают, 95+ Lighthouse
8. **Developer copy** — прямой, честный язык без маркетинг-мусора

---

## 8. Итоговые рекомендации

### Рекомендуемый стек

```
Framework:      Next.js 15 (App Router, TypeScript)
Styling:        Tailwind CSS v4
UI Components:  shadcn/ui (базовые)
Animations:     Motion (Framer Motion) — основной
                Aceternity UI — animated landing components
                Magic UI — дополнительные эффекты
Scroll:         Motion whileInView + CSS Scroll-Driven Animations
Backgrounds:    Aceternity UI (spotlight, grid, aurora) + custom CSS
Icons:          Lucide React
Fonts:          Geist (или Inter) + Cal Sans (display)
Deployment:     Vercel
Analytics:      Vercel Analytics + Plausible/PostHog
```

### Конкретный pipeline действий

```
День 1-2: ПЛАНИРОВАНИЕ
├── Определить ICP и value proposition
├── Написать копирайтинг всех секций
├── Собрать референсы (5+ сайтов)
├── Создать текстовый wireframe
└── Определить цветовую палитру и шрифты

День 3: SETUP
├── Инициализировать Next.js проект
├── Настроить Tailwind v4, shadcn/ui
├── Установить Motion, Aceternity UI, Magic UI
├── Создать CLAUDE.md с правилами проекта
├── Настроить дизайн-систему (CSS variables, компоненты)
└── Создать layout (fonts, metadata, providers)

День 4-6: СБОРКА СЕКЦИЙ (по одной за промпт в Claude Code)
├── Navbar (floating, backdrop-blur)
├── Hero (animated headline, CTA, background effects)
├── Social Proof (logo marquee)
├── Features (bento grid, animated cards)
├── How It Works (steps visualization)
├── Demo/Preview (audio player, interactive)
├── Testimonials (carousel)
├── Pricing (3 tiers)
├── FAQ (accordion)
├── Final CTA
└── Footer

День 7: АНИМАЦИИ И POLISH
├── Scroll-triggered animations (whileInView)
├── Hover effects на карточках
├── Staggered animations
├── Background effects (gradient, particles)
├── Smooth scroll (Lenis)
└── Micro-interactions (buttons, links)

День 8: ОПТИМИЗАЦИЯ
├── Performance (Lighthouse 90+)
├── SEO (metadata, OG, JSON-LD)
├── Mobile responsive (все breakpoints)
├── Accessibility (a11y audit)
├── Cross-browser testing
└── Deploy на Vercel

День 9-10: ИТЕРАЦИЯ
├── Получить фидбек
├── A/B тест hero variants
├── Добавить analytics
└── Финальные правки
```

### Библиотеки для каждого аспекта

| Аспект | Библиотека | Зачем |
|--------|-----------|-------|
| UI-компоненты | shadcn/ui | Базовые: Button, Dialog, Card, Input |
| Animated sections | Aceternity UI | Hero backgrounds, text effects, cards |
| Animated elements | Magic UI | Marquee, number ticker, particles |
| Анимации | Motion (Framer Motion) | whileInView, layout, gestures |
| Complex timelines | GSAP + ScrollTrigger | Если нужен parallax, pinning |
| Smooth scroll | Lenis | Плавный скролл страницы |
| Icons | Lucide React | Consistent, lightweight |
| Fonts | next/font | Оптимизированная загрузка |
| Forms | react-hook-form + zod | Валидация контактной формы |
| Toast/Notifications | sonner | Уведомления |
| Analytics | @vercel/analytics | Метрики |

### Чего избегать

1. **❌ Не просить Claude Code сделать весь сайт одним промптом** — будет каша. Секция за секцией!
2. **❌ Не перегружать анимациями** — 2-3 эффекта на экран максимум. Каждая анимация должна иметь цель
3. **❌ Не использовать тяжёлые 3D/WebGL** без необходимости — убивает performance на мобильных
4. **❌ Не игнорировать mobile** — 60%+ трафика. Тестировать каждую секцию
5. **❌ Не хардкодить контент** — вынести тексты в constants/CMS для A/B тестов
6. **❌ Не использовать stock фото** — продуктовые скриншоты, illustrations, абстрактные визуалы
7. **❌ Не забывать accessibility** — тёмная тема ≠ низкий контраст. WCAG AA минимум
8. **❌ Не деплоить без meta-тегов** — OG image, description, title для каждой страницы
9. **❌ Не использовать "use client" везде** — Server Components по умолчанию, клиентские только для интерактива
10. **❌ Carousel для фич** — bento grid конвертит лучше. Carousel = скрытый контент

---

## Бонус: Промпты для Claude Code

### Начальный промпт (после setup)
```
Создай Hero секцию для лендинга B2B SaaS продукта — голосовые AI-ассистенты.

Дизайн:
- Dark mode (#0A0A0A фон)
- Animated badge сверху: "Powered by GPT-4o" с shimmer border (Magic UI)
- H1: "Голосовые AI-ассистенты для вашего бизнеса" — с text-generate effect
- Подзаголовок: серый текст, fade-in с задержкой
- 2 кнопки: "Попробовать бесплатно" (gradient purple→blue) и "Смотреть демо" (outline)
- Фон: grid pattern (Aceternity) + subtle spotlight glow следующий за курсором
- Внизу: marquee с логотипами клиентов (Magic UI Marquee)

Стек: Next.js 15, TypeScript, Tailwind v4, Motion, Aceternity UI
Компонент должен быть Server Component где возможно, анимированные части — отдельные Client Components.
```

### Промпт для Features секции
```
Создай Features секцию в формате Bento Grid.

6 карточек разного размера (2 большие, 4 маленькие):
1. [Большая] Мультиязычность — 100+ языков. Animated globe (Magic UI)
2. [Большая] Аналитика звонков — дашборд preview с числами (number ticker)
3. [Маленькая] 24/7 доступность — icon + pulse animation
4. [Маленькая] Интеграции — logos CRM систем
5. [Маленькая] < 300ms latency — speed metric с animated counter
6. [Маленькая] Enterprise security — shield icon + checkmarks

Каждая карточка:
- Dark card (#111) с subtle border (white/10)
- Hover: scale 1.02 + border glow акцентным цветом
- whileInView: fade-up + stagger 0.1s
- Responsive: 1 колонка mobile, 2 tablet, 3 desktop (bento layout)
```

---

*Документ подготовлен на основе анализа актуальных источников за 2025-2026 год. Рекомендации протестированы на реальных проектах и соответствуют текущим best practices индустрии.*
