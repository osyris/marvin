# Memory — 2026-03-11

## Крон / напоминания — статус

- **Проблема с кроном решена** (частично): апдейт OpenClaw откатывал `agents.defaults.model.primary` обратно на `openai-codex/gpt-5.3-codex` → крон-задачи падали с "OAuth token refresh failed for openai-codex"
- **Исправление**: вручную перезаписал модель на `anthropic/claude-sonnet-4-6` в `/data/.openclaw/openclaw.json` после каждого апдейта
- **Важно**: после каждого `openclaw update` нужно проверять и при необходимости менять дефолтную модель обратно на Claude
- Тест в 04:46 UTC — предположительно прошёл (KZ написал "Отлично" до этого)

## Новые напоминания поставлены сегодня

- `fc740644` — 20 марта 08:00 UTC: убедиться что назначена дата ретроспективы с Наташей

## Архитектура auth — текущее состояние

- Gateway работает в `trusted-proxy` режиме (KZ хочет оставить)
- Внутренние инструменты (cron tool) подключаются напрямую к :8082 — не через auth-proxy
- Auth-proxy (:8080) инжектит `x-forwarded-user` только для браузерных запросов
- **Workaround**: указывать `model: anthropic/claude-sonnet-4-6` явно в крон-задачах (или менять дефолт)
- Loopback bypass в auth-proxy не реализован (KZ пока не подтвердил делать)

## Новый скилл после апдейта

- `pages-api` — скилл для динамических API эндпоинтов по пути `/pages-api/`
- Публичные (без авторизации), hot-reload, Express-like API
- KZ спросил про приватные страницы — `/pages/` публичные, `/dashboard/` требует gateway логин
- KZ хочет разобраться с приватными страницами — разговор прерван, продолжить

## OpenClaw версия

- Установлено несколько апдейтов за вечер 9-10 марта
- Текущая версия: предположительно 2026.3.9 (видно в session_status)

## Страницы KZ (текущие)

- `/pages/` — главная
- `/pages/bali-prep.html` — чеклист Bali
- `/pages/bali-bikes/` — байки Убуд
- `/pages/bali-hotels/` — отели Bali
